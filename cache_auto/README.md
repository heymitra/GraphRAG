# cache_auto/

LLM and embedding response cache for **auto-tuned** GraphRAG runs.

Same structure and purpose as `cache/` (see that README), but scoped to the
`settings.auto.yaml` configuration.

**Safe to delete** — the pipeline will rebuild it automatically.

> **Generated — do not commit.**
